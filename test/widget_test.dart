// Placeholder — real widget tests require Firebase/provider setup.
// The default counter smoke test was removed because this app uses SharapApp,
// not MyApp, and requires ProviderScope + Firebase initialization.
void main() {}
