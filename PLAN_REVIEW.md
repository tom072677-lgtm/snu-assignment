# PLAN_REVIEW

계획 리뷰 스크립트 실행 생략:
- 현재 환경에 `~/.Codex/scripts/codex_plan_review.ps1`가 없다.

수동 검토:
- 웹 푸시 알림은 앱이 꺼져 있어도 표시 가능하지만, 사용자가 지우는 것을 앱이 완전히 막을 수는 없다.
- 초 단위 애니메이션은 네이티브 알림에서 불가능하므로, 동일 tag의 hourly 갱신과 텍스트 진행 바가 현실적인 구현이다.
- 서버는 이미 구독별 과제 목록을 갖고 있으므로, 타깃 시간을 24~1시간으로 늘리는 것이 가장 작은 변경이다.
